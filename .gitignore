node_modules/
dist/
.wrangler/
.turbo/
*.local
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# local dev / test artifacts
.playwright-mcp/
dev-server.log
.cofferdam/cache/
coverage/

# nested git worktrees created by agent tooling — must stay out of biome's
# config-discovery walk (each worktree's own biome.json otherwise collides
# with this root config)
.claude/worktrees/
